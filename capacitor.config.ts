import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mukkai.delivery',
  appName: 'foodie-delivery-frontend',
  webDir: 'dist'
};

export default config;
